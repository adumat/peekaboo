apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "peekaboo.fullname" . }}
  labels:
    {{- include "peekaboo.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
