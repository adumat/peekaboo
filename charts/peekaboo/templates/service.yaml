apiVersion: v1
kind: Service
metadata:
  name: {{ include "peekaboo.fullname" . }}
  labels:
    {{- include "peekaboo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "peekaboo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
