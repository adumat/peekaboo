# Roadmap

Ideas and deferred work, roughly in priority order. Not commitments.

## UI

- [ ] Per-camera zoom & pan (persisted per camera) — was in the original build.
- [ ] Layout toggle for the video tiles (stacked ↔ side-by-side).
- [ ] Remember the last camera selection across sessions.

## Audio

- [ ] Configurable bitrate.

## Packaging & ops

- [ ] Multi-arch image (add `linux/arm64`).
- [ ] Publish the Helm chart to an OCI registry.
- [ ] Health/readiness endpoint distinct from `/`.

## Design

- [ ] A proper icon to replace the placeholder (commission or AI-generate).

## Nice to have

- [ ] Optional built-in auth / documented reverse-proxy + OIDC setup.
